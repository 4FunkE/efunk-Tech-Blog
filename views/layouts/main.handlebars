<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FunkE Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

{{!-- main content of the webpage --}}
<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2">
      <h1>
        <a href="/">FunkE Tech Blog</a>
      </h1>
      <nav>
        <ul class="nav-list">
          {{!-- shown when logged in --}}
          {{#if logged_in}}
          <li><a href="/">Home</a></li>
          <li><a href="/dashboard">Dashboard</a></li>
          <li><button class="no-button" id="logout">Logout</button></li>
          {{!-- shown when logged out --}}
          {{else}}
          <li><a href="/login">Login</a></li>
          {{/if}}
        </ul>
    </header>

    {{!-- responsive contanier that will fill other handlebars layouts --}}
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer class="flex-column justify-space-around">
      <p>FunkE information ONLY!</p>
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>